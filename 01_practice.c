#include <stdio.h>

int main(){
    // giving lenght and breadth to the rectangle

    int length = 3,breadth = 8;
    // writing the area formula

    int area = length*breadth; 
    // showing the output
    
    printf("The area of the rectangle is %d", area);
     return 0;
}