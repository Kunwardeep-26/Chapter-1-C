#include <stdio.h>

int main(){

    // giving the radius to the circle

    int radius = 3;
    // giving the value of pie

    float pi = 3.14;
    // showing the output

    printf("The area of this circle is %f", pi*radius*radius);
    // giving the height to the cylinder

    int height = 3;
    // showing the result

    printf("The area of your cylinder is %f\n", pi*radius*radius*height);
     return 0;
}